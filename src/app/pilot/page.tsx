import PilotPageClient from "@/components/pilot/PilotPageClient";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function PilotPage() {
  return <PilotPageClient />;
}
